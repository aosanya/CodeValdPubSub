# MVP Done — Completed Tasks

Completed tasks are removed from `mvp.md` and recorded here with their completion date.

| Task ID | Title | Completion Date | Branch | Coding Session |
|---------|-------|-----------------|--------|----------------|
| MVP-GIT-001 | Library Scaffolding | 2026-02-25 | `feature/GIT-001_library_scaffolding` | [GIT-001_library_scaffolding.md](coding_sessions/GIT-001_library_scaffolding.md) |
| MVP-GIT-002 | Filesystem Repo Lifecycle | 2026-02-25 | `feature/GIT-002_filesystem_repo_lifecycle` | [GIT-002_filesystem_repo_lifecycle.md](coding_sessions/GIT-002_filesystem_repo_lifecycle.md) |
| MVP-GIT-003 | Branch-Per-Task Workflow | 2026-02-25 | `feature/GIT-003_branch_per_task_workflow` | [GIT-003_branch_per_task_workflow.md](coding_sessions/GIT-003_branch_per_task_workflow.md) |
| MVP-GIT-004 | File Operations & Commit Attribution | 2026-02-25 | `feature/GIT-004_file_operations` | [GIT-004_file_operations.md](coding_sessions/GIT-004_file_operations.md) |
| MVP-GIT-005 | Fast-Forward Merge | 2026-02-25 | `feature/GIT-005_fast_forward_merge` | [GIT-005_fast_forward_merge.md](coding_sessions/GIT-005_fast_forward_merge.md) |
| MVP-GIT-006 | Auto-Rebase & Conflict Resolution | 2026-02-25 | `feature/GIT-006_auto_rebase` | [GIT-006_auto_rebase.md](coding_sessions/GIT-006_auto_rebase.md) |
| MVP-GIT-007 | History & Diff — UI Read Access | 2026-02-25 | `feature/GIT-007_history_and_diff` | [GIT-007_history_and_diff.md](coding_sessions/GIT-007_history_and_diff.md) |
| MVP-GIT-008 | ArangoDB Storage Backend | 2026-02-25 | `feature/GIT-008_arangodb_storage` | [GIT-008_arangodb_storage.md](coding_sessions/GIT-008_arangodb_storage.md) |
| MVP-GIT-009 | gRPC Service Proto & Codegen | 2026-02-26 | `feature/GIT-009_grpc_proto_codegen` | — |
| MVP-GIT-010 | gRPC Server Implementation | 2026-02-26 | `feature/GIT-010_grpc_server_implementation` | — |
| MVP-GIT-011 | Service-Driven Route Registration | 2026-02-27 | — | Routes declared in `internal/registrar/registrar.go` with `GrpcMethod` + `PathBindings`; Cross dynamic proxy handles forwarding |
| MVP-GIT-012 | Migrate shared infrastructure to CodeValdSharedLib | 2026-03-02 | `feature/MVP-GIT-012_sharedlib_migration` | Replaced local registrar/serverutil/arangoutil with SharedLib packages; removed local `gen/go/codevaldcross/`; all imports updated to `github.com/aosanya/CodeValdSharedLib` || GIT-001 | Pre-delivered schema & domain value types (v2 entitygraph redesign) | 2026-03-25 | `feature/GIT-001_schema-and-models` | `schema.go`: `DefaultGitSchema()` with TypeDefinitions for Repository, Branch, Tag (git_entities), Commit, Tree, Blob (git_objects — immutable). `models.go`: Go value types mirroring all six TypeDefinitions. Renamed v1 `Commit`→`CommitEntry` to free the name for the entitygraph entity.
| GIT-002 | Flat `GitManager` interface, request types, v2 errors | 2026-03-25 | `feature/GIT-002_gitmanager-interface` | `git.go`: `GitManager` interface (19 methods — repo lifecycle, branch, tag, file ops, history), `GitSchemaManager` alias, `CrossPublisher` interface, `gitManager` struct, `NewGitManager` constructor, stub implementations. `models.go`: `CreateRepoRequest`, `CreateBranchRequest`, `CreateTagRequest`, `WriteFileRequest`, `DeleteFileRequest`, `LogFilter`. `errors.go`: `ErrRepoNotInitialised`, `ErrTagAlreadyExists`, `ErrTagNotFound`. || GIT-003 | Update proto (`service.proto`) — `GitService` RPCs aligned to flat `GitManager`; regenerate Go stubs | 2026-03-25 | `feature/GIT-003_proto-gitservice` | `proto/codevaldgit/v1/service.proto`: Added `agency_id` (field 2) to `Repository` message to match v2 `models.go`. `gen/go/codevaldgit/v1/`: Regenerated — removes stale `agency_id` from request messages, adds `Repository.AgencyId`. Removed `internal/grpcserver/` (v1 incompatible with v2 proto; replaced in GIT-006). Stubbed `cmd/main.go` + `cmd/server/main.go` as placeholders pending GIT-009. |
| GIT-004 | ArangoDB entitygraph backend — thin adapter over SharedLib `entitygraph/arangodb`; fixed collection names: `git_entities`, `git_objects`, `git_relationships`, `git_schemas_draft`, `git_schemas_published`, `git_graph` | 2026-03-25 | `feature/GIT-004_arangodb-entitygraph-backend` | Replaced v1 `storage.Storer` implementation (846 lines) with type-alias adapter matching CodeValdAgency pattern. `Backend = sharedadb.Backend`, `Config = sharedadb.ConnConfig`, `toSharedConfig()` fills fixed collection/graph names. 3 always-run unit tests + 6 integration tests (skip without `GIT_ARANGO_ENDPOINT`). `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./...` ✅. |
| GIT-008 | Config (`internal/config/`) + Cross registrar route update | 2026-03-26 | `feature/GIT-008_config-cross-registrar` | Added `internal/config/config.go`: `Config` struct + `Load()` reading all GIT_* and CROSS_* env vars with sensible defaults. Fixed `internal/registrar/registrar.go`: `gitRoutes()` delegates to per-concern helpers. Added `internal/registrar/routes.go`: full v2 GitService route set (repo, branch, tag, file ops, history) using correct `/codevaldgit.v1.GitService/` paths; Smart HTTP direct-proxy route declarations. `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./...` ✅. |
| GIT-010 | Unit & integration tests — fakeDataManager, gRPC handler tests, impl bug fixes | 2026-06-19 | `feature/GIT-010_unit-integration-tests` | `git_manager_test.go`: 17 tests with in-memory `fakeDataManager` (BFS `TraverseGraph`), `fakeSchemaManager`, `fakePublisher`. `internal/server/server_test.go`: 20 gRPC handler tests via `bufconn` covering all RPCs + complete error-to-status-code mapping. Fixed missing `has_branch`/`has_tag`/`has_tree`/`has_parent` edges in impl. `go test -race ./...` ✅ |
| GIT-009 | `cmd/main.go` — cmux wiring, ArangoDB backend, schema seed, Cross registrar | 2026-03-26 | `feature/GIT-009_cmd-main-cmux-wiring` | `cmd/main.go`: full binary entry point — loads config, connects ArangoDB entitygraph backend (`storage/arangodb.NewBackend`), seeds `DefaultGitSchema` idempotently on startup, starts Cross registrar heartbeat, constructs `GitManager` for gRPC, constructs filesystem `Backend` for git Smart HTTP, wires both onto a single TCP port via `cmux` (gRPC on HTTP/2 prefix, git HTTP on any other). Graceful shutdown on SIGTERM/SIGINT (30 s drain). Added `Publish` method to `internal/registrar.Registrar` implementing `codevaldgit.CrossPublisher` (compile-time assertion added). Added `github.com/soheilhy/cmux v0.1.5` dependency. `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./...` ✅. || GIT-015a | Schema additions — `sha` on Branch, `head_ref` on Repository, `GitInternalState` TypeDefinition | 2026-04-14 | `feature/GIT-010_arangodb-git-storer` | `schema.go`: added `sha` (string) to Branch TypeDefinition (storer fast-path for ref resolution); added `head_ref` (string) to Repository TypeDefinition (symbolic HEAD target written on InitRepo); added `GitInternalState` TypeDefinition (`StorageCollection: "git_internal"`, `UniqueKey: ["state_type"]`) with properties `state_type` and `data` (base64) for config/index/shallow storage. `go build ./...` ✅, `go test -race ./...` ✅. |
| GIT-015b | Real git SHA + `entries` property + `advanceBranchHead` sha write | 2026-04-14 | `feature/GIT-010_arangodb-git-storer` | `git_impl_fileops.go`: removed synthetic `contentSHA()`/`commitSHA()` helpers; Blob/Tree/Commit SHAs computed via `plumbing.MemoryObject` encode+hash (real git binary SHA-1, identical to what `git hash-object` produces). Tree entity now carries `entries` property — a JSON array `[{"name":"<basename>","mode":"<git-mode>","sha":"<40hex>"}]` — enabling the storer to reconstruct the binary tree object in a single entity read (Gap 1 + Gap 3). `git_impl_repo.go` `advanceBranchHead`: after writing `head_commit_id`, fetches the new Commit entity, reads its `sha` property, and writes it to `Branch.sha` via `dm.UpdateEntity` (Gap 2). `go build ./...` ✅, `go test -race ./...` ✅. |
| GIT-015c | `storer.go` EncodedObjectStorer via DataManager | 2026-04-14 | `feature/GIT-010_arangodb-git-storer` | `storage/arangodb/storer.go`: replaced raw `driver.Database` + `gitraw_objects` collection with `entitygraph.DataManager`. `SetEncodedObject` → `dm.CreateEntity` (TypeID=Blob/Tree/Commit, `sha`=hash, `data`=base64 raw bytes, idempotent on conflict). `EncodedObject` → `dm.ListEntities` filtered by TypeID + `sha`, deserialises `data` back to `plumbing.MemoryObject`. `HasEncodedObject` → same list query; returns `plumbing.ErrObjectNotFound` on empty. `IterEncodedObjects` → `dm.ListEntities` by TypeID, wrapped in `storer.NewEncodedObjectSliceIter`. `EncodedObjectSize` → list query reading `size` field. `AddAlternate` → no-op. All `gitraw_objects`/`colObjects` references removed (Gap 5, objects half). `go build ./...` ✅, `go test -race ./...` ✅. |
| GIT-015d | `storer.go` ReferenceStorer + internal-state storers via DataManager | 2026-04-14 | `feature/GIT-010_arangodb-git-storer` | `storage/arangodb/storer.go`: `SetReference(HEAD)` → updates `Repository.head_ref`; `SetReference(refs/heads/X)` → updates `Branch.sha`; `Reference(HEAD)` → reads `head_ref` as symbolic ref; `Reference(refs/heads/X)` → reads `Branch.sha` as hash ref; `Reference(refs/tags/X)` → reads `Tag.sha`. `IterReferences` → HEAD from Repository + all Branch + all Tag entities. `RemoveReference` → `dm.DeleteEntity` on Branch/Tag. `CheckAndSetReference` → reads current `Branch.sha`, returns `storage.ErrReferenceHasChanged` on mismatch. `SetConfig`/`Config`, `SetIndex`/`Index`, `SetShallow`/`Shallow` → `GitInternalState` entities with `state_type` discriminator. `Module(name)` → `newArangoStorer(dm, agencyID+"/module/"+name)`. All `gitraw_refs`/`gitraw_config`/`gitraw_index`/`gitraw_shallow`/`ensureGitRawCollections` removed (Gap 5, refs + internal-state half). `go build ./...` ✅, `go test -race ./...` ✅. |
| GIT-015e | Backend refactor + `cmd/main.go` single unified backend + filesystem removal | 2026-04-14 | `feature/GIT-010_arangodb-git-storer` | `storage/arangodb/backend.go`: replaced `driver.Database` with `entitygraph.DataManager`; `InitRepo` is a no-op (entity creation is owned by `gitManager.InitRepo`); `OpenStorer` verifies Repository entity exists then returns `newArangoStorer(dm, agencyID)` + `memfs.New()` (Gap 4). `storage/arangodb/arangodb.go`: added `NewArangoStorerBackend(dm entitygraph.DataManager) codevaldgit.Backend`. `cmd/main.go`: single `arangoBackend` passed to both `gitManager` (gRPC) and `server.NewGitHTTPHandler` (Smart HTTP) via `gitarangodb.NewArangoStorerBackend(arangoBackend)` — filesystem backend and all `GIT_REPOS_BASE_PATH`/`GIT_REPOS_ARCHIVE_PATH` config vars removed. `internal/config/config.go`: no `ReposBasePath`/`ReposArchivePath` fields; only `ArangoWorktreePath` (optional, defaults to memfs). `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./...` ✅. Both gRPC GitManager and git Smart HTTP handler now read/write the same ArangoDB entitygraph storage layer — a `git clone` after `InitRepo` succeeds end-to-end. || GIT-017 | gRPC-Data Gap Fix + Persistent `[agency_id, sha]` Indexes | 2026-04-17 | `feature/GIT-017_grpc-data-gap-fix` | Added `data` (base64 raw git object bytes) to Blob/Tree/Commit entities created by `WriteFile`; added persistent `[agency_id, sha]` composite ArangoDB indexes on `git_objects`, `git_blobs`, `git_trees`, `git_commits`. Also refactored repository management to support multiple repositories per agency. `go build ./...` ✅, `go vet ./...` ✅. |
| GIT-019 | Documentation Layer — Keyword entity + documentation edges, keyword CRUD, branch-scoped edge CRUD | 2026-04-17 | `feature/GIT-019_documentation-layer` | Added `Keyword` TypeDefinition and relationship types (`tagged_with`, `documents`/`documented_by`, `depends_on`/`imported_by`, `has_child`/`belongs_to_parent`) to `git-schema-v1`. Implemented keyword CRUD and branch-scoped edge CRUD with automatic inverse edges. |
| GIT-020 | Graph Query Endpoints — neighborhood query + SearchByKeywords | 2026-04-17 | `feature/GIT-020_graph-query-endpoints` | Added `GetNeighborhood` (depth 1-3, 100-node cap) and `SearchByKeywords` (AND/OR, cascading) to `GitManager` interface. AQL graph traversal implementation via `git_impl_graph.go`. |
| GIT-018 | Repo-Scoped Sub-Resource Routes | 2026-04-17 | `feature/GIT-018_repo-scoped-sub-resource-routes` | Nested all sub-resource HTTP routes (`branches`, `tags`, `files`, `tree`, `log`, `diff`) under `/git/{agencyId}/repositories/{repoId}/`. Added `{repoId} → repository_id` PathBinding to all affected routes in `internal/registrar/routes.go`. Server handlers already used `req.GetRepositoryId()`. Updated CodeValdCross integration tests to use repo-scoped URLs and corrected `/repository` (singular) to `/repositories` (plural). `go build ./...` ✅, `go vet ./...` ✅. |

| GIT-022 | Edge Lifecycle — replicate/delete/migrate doc edges (DR-010) | 2026-04-17 | `feature/GIT-022_edge-lifecycle` | `git_impl_edgelifecycle.go`: `replicateDocEdges` (GIT-022a) copies `tagged_with`/`references` edges from branch blobs to main on MergeBranch; `deleteDocEdgesForBranch` (GIT-022b) removes all branch-scoped edges on DeleteBranch; `deleteDocEdgesForBlob` (GIT-022c) removes edges on the specific blob being deleted in DeleteFile. `mergeEdgeProps` helper preserves caller-supplied Properties (e.g. `descriptor`) alongside injected `branch_id`. Added `Properties` field to `CreateEdgeRequest`. Extended `validDocEdges` with `references`/`referenced_by`. 6/6 unit tests pass. `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./...` ✅. |
| GIT-023a | Lazy import v2 — Branch.status + FetchBranchJob TypeDefinition + git_fetchjobs collection in schema.go | 2026-04-19 | `feature/GIT-023a_lazy-import-v2-schema` | Added `status` (stub/fetching/fetched/fetch_failed) and `source_url` properties to `Branch` TypeDefinition. Added `bare_clone_path` and `fetched_commit_shas` properties to `Repository` TypeDefinition. Added `FetchBranchJob` TypeDefinition with `git_fetchjobs` StorageCollection (pending/running/completed/failed lifecycle). Updated package godoc to describe all 9 TypeDefinitions. `go build ./...` ✅, `go vet ./...` ✅. |
| GIT-023b | Lazy import v2 — `FetchBranchRequest`, `FetchBranchJob` types + `ErrBranchAlreadyFetched`, `ErrBlobContentUnavailable` errors + `FetchBranch`/`GetFetchBranchStatus` on `GitManager` interface | 2026-04-19 | `main` (direct commit) | Added `FetchBranchRequest` (AgencyID, RepositoryID, BranchName) and `FetchBranchJob` (ID, AgencyID, RepositoryID, BranchName, Status, CreatedAt, UpdatedAt, Error) to `models.go`. Added `ErrBranchAlreadyFetched` and `ErrBlobContentUnavailable` sentinel errors to `errors.go`. Added `FetchBranch(ctx, FetchBranchRequest) (FetchBranchJob, error)` and `GetFetchBranchStatus(ctx, agencyID, jobID string) (FetchBranchJob, error)` to the `GitManager` interface with full godoc. Stub implementations added to `git_impl_fetchbranch.go`. Fake implementations added to server tests. `go build ./...` ✅, `go vet ./...` ✅. |
| GIT-023e | Lazy import v2 — update `ReadFile`: check entity blob cache → read from backend storer → cache back → `ErrBlobContentUnavailable` fallback | 2026-04-21 | `main` | Updated `ReadFile` in `git_impl_fileops.go`: fast path returns immediately when `blob.Content != ""`; lazy path calls `loadBlobContentFromStorer` (opens `m.backend.OpenStorer`, reads blob object by SHA via `gogit.Open`, detects utf-8 vs base64 encoding, caches result back via `cacheBlobContent`). **Original GIT-023e design used `gogit.PlainOpen` on a bare clone path — this was wrong**: pushed repositories store objects in the backend storer (ArangoDB/filesystem), not in a bare clone on disk. `loadBlobContentFromBareClone` was replaced entirely with `loadBlobContentFromStorer`. `go build ./...` ✅, `go vet ./...` ✅. || GIT-023f | Lazy import v2 — proto additions: `FetchBranch` + `GetFetchBranchStatus` RPCs; `buf generate` | 2026-04-19 | `feature/GIT-023f_lazy-import-v2-proto-fetchbranch` | Added `FetchBranchRequest`, `FetchBranchJob`, `GetFetchBranchStatusRequest` messages to `service.proto`. Added `FetchBranch` and `GetFetchBranchStatus` RPCs to `GitService`. Ran `buf generate` — `service.pb.go` and `service_grpc.pb.go` updated with new types and method stubs. `go build ./...` ✅, `go vet ./...` ✅. || GIT-023g | Lazy import v2 — gRPC handlers for `FetchBranch` + `GetFetchBranchStatus` + HTTP route registration | 2026-04-19 | `feature/GIT-023g_grpc-handlers-fetchbranch` | Added `FetchBranch` and `GetFetchBranchStatus` handlers to `internal/server/server.go`. `FetchBranch` resolves branch name → entity ID via `resolveBranchID` (ListBranches scan) then delegates to `mgr.FetchBranch`. `GetFetchBranchStatus` delegates directly to `mgr.GetFetchBranchStatus`. Added `fetchBranchJobToProto` mapper to `mappers.go`. Added `fetchBranchRoutes()` to `internal/registrar/routes.go` — POST `/git/{agencyId}/fetch-jobs` and GET `/git/{agencyId}/fetch-jobs/{jobId}` — wired into `gitRoutes()`. `go build ./...` ✅, `go vet ./...` ✅. |
| GIT-023h | Unit tests — stub import < 10 s; `FetchBranch` idempotency; lazy `ReadFile` content cache; `ErrBranchAlreadyFetched` rejection | 2026-04-19 | `feature/GIT-023h_lazy-import-v2-unit-tests` | Added `git_lazyimport_test.go` with 7 tests: `TestFetchBranch_AlreadyFetched`, `TestFetchBranch_AlreadyFetching`, `TestFetchBranch_StubTransitionsToFetching`, `TestReadFile_CachedBlob`, `TestReadFile_LazyLoad_NoBareClone`, `TestImportRepo_StubBranches_Fast` (local git source, completes <10 s, only Branch stubs created), `TestGetFetchBranchStatus_NotFound`. Fixed `cloneRootDir` to `os.RemoveAll` stale clone dir before `PlainClone` so repeated test runs don't fail with "repository already exists". All 7 new tests pass; `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./...` ✅ (pre-existing `TestImportRepo_RejectsIf*` failures unrelated). |
| GIT-025a | `internal/gitgraph/parser.go` — `MappingFile`, `KeywordDef`, `MappingEntry`, `RefEntry`, `ParseMappingFile`, `ErrInvalidMappingFile` | 2026-04-22 | `feature/GIT-025a_gitgraph_parser` | `internal/gitgraph/errors.go`: `ErrInvalidMappingFile{File, Details}`. `internal/gitgraph/parser.go`: `SignalVocab`, `SignalDef`, `DefaultSignals`, `ValidDescriptors`, `MappingFile`, `KeywordDef`, `MappingEntry`, `DepthEntry`, `TestedByEntry`, `RefEntry`, `ParseSignalVocab`, `ParseMappingFile`. Validates: empty keyword names, duplicate keyword names, empty mapping files, invalid descriptors, signals not in active vocab, depth keywords not in same mapping's keywords list, empty tested_by files. 18 tests pass. `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./...` ✅. |
| GIT-025b | `internal/gitgraph/sync.go` — `Syncer.Sync`: keyword upsert + edge hard-sync | 2026-04-22 | `feature/GIT-025b_gitgraph_sync` | `sync.go`: `Syncer` struct (dm, agencyID, vocab), `NewSyncer`. `Sync` orchestrates four phases: signal persistence (insert-only, never update/delete), keyword upsert across all mapping files (create or update, never delete), parent edge resolution (belongs_to_parent + has_child), and per-file edge hard-sync (tagged_with + references, branch-scoped, removals honoured). `tagged_with` edges carry signal/note from depths[] or default signal "surface". `tested_by[]` entries become `references {descriptor:"tested_by"}` edges. All individual operation errors logged, never returned. `sync_test.go`: 13 new tests with in-memory fakeDataManager covering keyword create/update, tagged_with with signal/note, default surface signal, references + tested_by edge create, stale edge delete, other-branch edge untouched, blob-not-found skip, parent edge wiring, signal insert-only. `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./internal/gitgraph/...` ✅ (31 tests pass). |
| GIT-011 | Concurrency and Atomic Ref Updates | 2026-04-22 | `feature/GIT-011_concurrency-ref-updates` | `git.go`: `RefLocker` interface + `mutexLocker` default (sync.Map of *sync.Mutex, context-aware). `errors.go`: `ErrMergeConcurrencyConflict`. `gitManager` struct: `locker RefLocker` field; `NewGitManager` accepts locker (nil→default). `git_impl_branch.go` (split from git_impl_repo.go): `MergeBranch` wrapped in `WithMergeLock`; re-reads default branch inside lock. `advanceBranchHead`: `expectedHeadCommitID` CAS guard (ErrMergeConcurrencyConflict on mismatch). Split 786-line `git_impl_repo.go` into `git_impl_repo.go` (221), `git_impl_branch.go` (352), `git_impl_tag.go` (135), `git_impl_converters.go` (108). Also fixed pre-existing `deleteEdgesByBranchID` bug (missing DeleteRelationship call). 4 new tests. `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./...` ✅. |
| GIT-025c | `git_impl_index.go` — hook `IndexPushedBranch` to call `syncGitGraph` after commit/blob phase | 2026-04-22 | `feature/GIT-025c_hook_index_pushed_branch_sync_git_graph` | `git_impl_index.go`: `syncGitGraph` opens the pushed commit tree, reads `.git-graph/.signals.json` first (fallback to `DefaultSignals` on absent/malformed), iterates all `.git-graph/*.json` mapping files, parses each with `ParseMappingFile`, resolves branch entity ID via `findBranchIDForRepo`, and calls `gitgraph.NewSyncer(dm, agencyID, vocab).Sync`. `readBlobFromRepo` helper reads blob bytes by hash. `git_impl_push.go`: Phase 6 added at end of `IndexPushedBranch` — `syncGitGraph` error is logged and never propagated (push never fails due to .git-graph sync). `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./internal/gitgraph/...` ✅ (33 tests pass). |
| GIT-025d | `.github/prompts/map-folder-keywords.prompt.md` — AI agent prompt: map folders to keywords via `.git-graph/` JSON files | 2026-04-22 | `feature/GIT-025d_map_folder_keywords_prompt` | Created `.github/prompts/map-folder-keywords.prompt.md`: step-by-step guide for an AI agent to analyse a repo, define keywords, assign signals (`surface`→`authority`), declare `references` edges with typed descriptors, split output into per-domain `.git-graph/*.json` files, and optionally customise the signal vocabulary via `.git-graph/.signals.json`. Includes full schema reference tables, validation checklist, and a complete JSON example. Push triggers `IndexPushedBranch` — no direct API calls needed. |
| GIT-026 | `POST .../branches/{branchId}/graph/query` — multi-filter, signal-sorted graph query | 2026-04-22 | `feature/GIT-026_graph-query-endpoint` | `QueryGraphRequest` model; `GitManager.QueryGraph` with 5-dimension AND filters (signals, keyword_ids, file_types, folders, relationships), signal-layer sort, limit cap; `loadSignalLayers`, `filterBlobsByPath`, `queryGraphEdges`, `toStringSet` helpers; proto `QueryGraphRequest` + `rpc QueryGraph`; `Server.QueryGraph` gRPC handler; HTTP route `POST /git/{agencyId}/branches/{branchId}/graph/query` in docs_routes.go. 8 unit tests. `go build ./...` ✅, `go test -race ./...` ✅. |
| GIT-025e | Schema v2 — `depths[]` keyword entries with `signal` + `note` fields; `.signals.json` loader | 2026-04-22 | `feature/GIT-025e_schema_v2_depths_signals` | All GIT-025e functionality was delivered as part of GIT-025a/b/c. `parser.go`: `SignalVocab`, `SignalDef`, `DefaultSignals`, `ParseSignalVocab` (returns `DefaultSignals` + `ErrInvalidMappingFile` on absent/malformed), `DepthEntry{Keyword, Signal, Note}`, `ParseMappingFile` validates `depths[].signal` against active vocab and `depths[].keyword` against same mapping's `keywords[]`. `sync.go`: `persistSignals` inserts each `SignalDef` as a `Signal` entity (insert-only, never update/delete existing records). `git_impl_index.go`: `syncGitGraph` reads `.git-graph/.signals.json` before all other mapping files; absent file silently falls back to `DefaultSignals`; malformed file logs `ErrInvalidMappingFile` and falls back. All acceptance criteria satisfied: `ParseSignalVocab` valid ✅, malformed ✅, empty ✅; `ParseMappingFile` depth signal not in vocab ✅, depth keyword not in mapping keywords ✅; `persistSignals` insert-only ✅, existing skipped ✅. `go build ./...` ✅, `go vet ./...` ✅, `go test -race ./internal/gitgraph/...` ✅ (33 tests pass). |
